<idea-plugin>
    <id>com.alibaba.p3c.smartfox</id>
    <name>Zyzx Java Coding Guidelines</name>
    <description>中移在线代码规约插件.</description>

    <change-notes>
        <![CDATA[
         <ul>
        10.0.0
        <li>本插件基于Alibaba Java Coding Guidelines实现</li>
        <li>现根据中移在线Sonar规则，加入了目前公司在用的64条Bug级别的规则</li>
        <li>中移在线规则</li>
        <li>compareTo可能返回不是具体的值（除0外），建议用 >0、<0、=0)</li>
        <li>Double.longBitsToDouble返回给定的位所代表的double值,需要一个64位的long类型参数</li>
        <li>equals作为方法名应该仅用于重写Object.equals(Object)来避免混乱</li>
        <li>Iterator.next（）使迭代器前进一项。 因此，在Iterator.hasNext（）中调用它会破坏Iterator，并导致生产中出现意外行为</li>
        <li>PreparedStatement与ResultSet参数设置与获取数据由序号1开始而非0</li>
        <li>先要获得对象锁才能进行"wait", "notify" , "notifyAll"操作</li>
        <li>线程类应该重写run方法</li>
        <li>整型与长整型位移操作数应该介于1与类型占位数-1</li>
        <li>finally块中使用return, break, throw等跳转语句，会阻止在try catch中抛出的未处理异常的传播</li>
        <li>不要在foreach循环里进行元素的remove/add操作，remove元素请使用Iterator方式</li>
        <li>保证锁能够释放</li>
        <li>循环应该至少走一次</li>
        <li>不要在线程中使用"wait(…)", "notify()" and "notifyAll()"</li>
        <li>方法不应命名为“ tostring”，“ hashcode”或“ equal”,如果需这样命名则必须加@Override</li>
        <li>非public方法不要注解Transactional,调用时spring 会抛出异常</li>
        <li>非线程安全的字段不应该静态化。注：可改为静态方法返回对象</li>
        <li>打开的资源应该关闭并且放到finally块中进行关闭</li>
        <li>Servlet不应具有可变的实例字段</li>
        <li>序列化的类的非序列化父类应有一个无参构造器</li>
        <li>等…………</li>
        </ul>
        ]]>
    </change-notes>
    <vendor>zyzx</vendor>
    <version>10.0.0</version>
    <idea-version since-build="145.0"/>
    <depends optional="true">com.intellij.velocity</depends>
    <depends optional="true" config-file="p3c.xml">com.intellij.modules.java</depends>
    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.lang</depends>
    <depends>com.intellij.modules.vcs</depends>
    <depends>com.intellij.modules.xml</depends>
    <depends>com.intellij.modules.xdebugger</depends>
</idea-plugin>
